# 🔧 Устранение проблем

## Ошибка: ModuleNotFoundError: No module named 'flask'

**Проблема:** Python не может найти установленные модули.

**Решение:**
1. Убедись, что используешь правильный Python:
```bash
python --version
```

2. Установи зависимости заново:
```bash
pip install flask flask-cors keyboard pyautogui pystray pillow
```

3. Если не помогло, попробуй:
```bash
python -m pip install flask flask-cors keyboard pyautogui pystray pillow
```

## Иконка не появляется в трее

**Проблема:** Скрипт запускается, но иконки нет.

**Решение:**
- Проверь скрытые значки в трее (стрелка вверх возле часов)
- Перезапусти скрипт
- Проверь, что `pystray` и `pillow` установлены

## Команды не работают в Minecraft

**Проблема:** Кнопки нажимаются, но команды не выполняются.

**Решение:**
1. Убедись, что Minecraft в фокусе (активное окно)
2. Проверь, что чат открывается на клавишу `T` (английская раскладка)
3. Попробуй увеличить задержки в `mc_controller.py`:
```python
time.sleep(0.2)  # вместо 0.1
```

## Сервер недоступен (localhost:5000)

**Проблема:** Браузер не может подключиться к серверу.

**Решение:**
1. Проверь, что `mc_controller.py` запущен
2. Проверь иконку в трее
3. Открой http://localhost:5000/status в браузере
4. Если порт занят, измени в `mc_controller.py`:
```python
app.run(host='0.0.0.0', port=5001)  # другой порт
```

И в `app.js`:
```javascript
fetch('http://localhost:5001/command', {  // новый порт
```

## Кулдаун не сохраняется

**Проблема:** После перезагрузки страницы кулдаун сбрасывается.

**Решение:**
- Проверь, что браузер разрешает localStorage
- Не используй режим инкогнито
- Очисти кеш браузера и перезагрузи страницу

## Скрипт не запускается через .bat файл

**Проблема:** Двойной клик на `start_controller.bat` ничего не делает.

**Решение:**
1. Открой файл в блокноте и проверь путь к Python
2. Запусти через командную строку:
```bash
start_controller.bat
```
3. Посмотри на ошибки в консоли

## Нужны права администратора

**Проблема:** `keyboard` требует права администратора.

**Решение:**
- Запусти `start_controller.bat` от имени администратора (правый клик → "Запуск от имени администратора")
- Или используй `pyautogui` вместо `keyboard` для отправки команд

## Дополнительная помощь

Если проблема не решена:
1. Проверь логи в консоли Python
2. Проверь консоль браузера (F12)
3. Напиши в Telegram: [@imCEOmf](https://t.me/imCEOmf)
