# 🔧 Исправление проблем с подключением

## Проблема: "Не удалось подключиться к этому IP"

Это означает, что телефон не может достучаться до Flask сервера на ПК.

## Решение 1: Брандмауэр Windows (САМОЕ ЧАСТОЕ)

### Автоматическое исправление:
1. Запусти `fix_firewall.bat` **от имени администратора**
2. Нажми "Да" когда Windows спросит разрешение
3. Готово!

### Ручное исправление:
1. Открой "Панель управления"
2. "Система и безопасность" → "Брандмауэр Windows"
3. "Разрешение взаимодействия с приложением"
4. Нажми "Изменить параметры"
5. Найди "Python" в списке
6. Поставь галочки для "Частные" сети
7. Если Python нет - нажми "Разрешить другое приложение"
8. Найди `python.exe` (обычно в `C:\Users\[User]\AppData\Local\Programs\Python\`)
9. Добавь и разреши для частных сетей

## Решение 2: Проверь сеть

### ПК и телефон должны быть в ОДНОЙ сети:
- Оба подключены к одному Wi-Fi роутеру
- Или телефон подключен к ПК через USB (режим модема)

### Как проверить:
**На ПК:**
```bash
ipconfig
```
Найди "IPv4-адрес" в разделе Wi-Fi адаптера (например: 192.168.1.3)

**На телефоне:**
- Настройки → Wi-Fi → Нажми на подключенную сеть
- Посмотри IP адрес (должен быть в том же диапазоне: 192.168.1.X)

## Решение 3: Проверь что сервер запущен

1. На ПК должен быть запущен `mc_controller.py`
2. В консоли должно быть написано:
```
* Running on http://192.168.1.3:5000
```
3. Окно с кодом должно быть открыто

## Решение 4: Тест подключения

1. Открой `test_connection.html` на телефоне
2. Введи IP из окна MC Controller
3. Нажми "Проверить подключение"
4. Если ошибка - смотри что написано

## Решение 5: Попробуй другой порт

Если порт 5000 занят, измени в `mc_controller.py`:
```python
app.run(host='0.0.0.0', port=5001)  # Вместо 5000
```

И в окне будет показан новый порт.

## Решение 6: Отключи VPN

Если на ПК или телефоне включен VPN - отключи его.
VPN может блокировать локальные подключения.

## Решение 7: Проверь антивирус

Некоторые антивирусы блокируют входящие подключения.
Временно отключи антивирус и попробуй подключиться.

## Решение 8: Используй USB подключение

Если Wi-Fi не работает:
1. Подключи телефон к ПК через USB
2. Включи "USB-модем" в настройках телефона
3. На ПК появится новое сетевое подключение
4. Узнай новый IP через `ipconfig`
5. Используй этот IP

## Проверка что все работает:

### На ПК:
1. Запусти `mc_controller.py`
2. Окно показывает код и IP
3. В консоли: "Running on http://192.168.1.3:5000"

### На телефоне:
1. Открой браузер
2. Введи IP из окна (например: `http://192.168.1.3:5000`)
3. Должна открыться страница с вводом кода
4. Введи код из окна
5. Должно подключиться!

## Все еще не работает?

1. Перезагрузи роутер
2. Перезагрузи ПК и телефон
3. Попробуй другой браузер на телефоне
4. Проверь что на ПК нет других программ на порту 5000

---

**После исправления попробуй снова подключиться!** 🔧
