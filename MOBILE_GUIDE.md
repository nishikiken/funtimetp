# 📱 Подключение с телефона

## Проблема
Когда ты открываешь сайт на телефоне, он не может подключиться к `localhost` компьютера, потому что `localhost` - это сам телефон.

## Решение
Нужно использовать локальный IP адрес компьютера.

## Инструкция

### Шаг 1: Запусти MC Controller на ПК
```bash
python mc_controller.py
```

Появится окно с:
- Кодом доступа (например: `ABC-123`)
- IP адресом для телефона (например: `http://192.168.1.3:5000`)

### Шаг 2: Открой сайт на телефоне
На телефоне открой браузер и введи адрес из окна:
```
http://192.168.1.3:5000
```
(Твой IP будет другой - смотри в окне!)

Или открой файл через GitHub Pages и введи IP вручную.

### Шаг 3: Введи код
- Введи код из окна (например: A B C - 1 2 3)
- Если не подключается автоматически, появится поле для ввода IP
- Введи IP из окна (например: `192.168.1.3:5000`)
- Нажми "Подключиться к этому IP"

### Шаг 4: Используй
Теперь можешь управлять Minecraft с телефона!

## Важно

### Оба устройства должны быть в одной сети
- ПК и телефон должны быть подключены к одному Wi-Fi
- Или телефон подключен к ПК через USB (режим модема)

### Брандмауэр Windows
Если не подключается, разреши Python в брандмауэре:
1. Панель управления → Брандмауэр Windows
2. Разрешить приложение через брандмауэр
3. Найди Python и разреши для частных сетей

### Проверка IP адреса
Если IP в окне неправильный, узнай его вручную:
```bash
ipconfig
```
Ищи "IPv4-адрес" в разделе Wi-Fi адаптера.

## Пример

**Окно на ПК:**
```
┌────────────────────────────┐
│ Введите этот код на сайте  │
│                            │
│        ABC-123             │
│                            │
│ ─────────────────────────  │
│ Адрес для телефона:        │
│ http://192.168.1.3:5000    │
│                            │
│ Ожидание подключения...    │
└────────────────────────────┘
```

**На телефоне:**
1. Открой `http://192.168.1.3:5000`
2. Введи код: `A` `B` `C` `-` `1` `2` `3`
3. Готово!

---

**Теперь ты можешь управлять Minecraft с дивана!** 🎮📱
